# Components

Place your reusable UI components here.

## Structure:
- Each component should have its own folder
- Include the component file, styles (if any), and tests
- Export from index.ts for clean imports

## Example:
```
Button/
├── index.tsx
├── Button.tsx
├── Button.test.tsx
└── Button.module.css (optional)
```

